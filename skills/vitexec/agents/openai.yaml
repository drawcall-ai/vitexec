interface:
  display_name: "vitexec"
  short_description: "Inspect, control, and profile live Vite apps"
  default_prompt: "Use $vitexec to add script pages, inspect a live Vite app, or verify interactions through trusted input."
